

<% if @note.errors.any?%>

<h2>The following errors prevented the notes from getting created</h2>

<ul>

<% @note.errors.full_messages.each do |msg| %>

<li><%= msg %></li>

<% end %>

</ul>

<%end%>

<%= form_for @note do |f| %>
 <p> <div class="form-group">
    <label for="name">Name</label>
    <%= f.text_field :name %>  <%= f.submit "Submit" ,:class=>"btn btn-info"%> <p></p>
<p align="middle"><a href="<%=notes_path(:directory_id=>@dir_id)%>" class="btn btn-info" role="button">Go back notes</a>
  </div>   </p>
	<%= hidden_field_tag :directory_id, @dir_id  %>

  <div class="form-group">
    <label for="content"></label>
    <%= f.text_area :content , size: "200x100" %></p>
  </div>
 <%end%>